# Task 1 empirical stop-gate — PASS

- Gate UTC: `2026-07-24T15:24:20Z`–`2026-07-24T15:35:51Z`.
- Source commit: `6fe528d8036a13807ba63acfceedad987fa7f586`.
- Source tree: `76298765fdb4b352da1fd28bb9d6a30f2346eee2` (`git rev-parse 6fe528d^{tree}`).
- The gate began clean at that exact commit and ended clean at that exact commit. The next commit changes evidence only.
- Host/tooling: macOS `15.7.7` / `arm64`; Bun `1.3.14`; Rust/Cargo `1.96.1`; Docker `29.4.0`; Bats `1.13.0`; ShellCheck `0.11.0`.
- Host tmux: `/opt/homebrew/bin/tmux`, `tmux 3.7b`, SHA-256 `17ed04318c6fd2b12e04bce0130425cbe484cbef9cffd2febfba90425e5ad7d0`.
- Exact OpenCode: `1.17.20`, SHA-256 `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`.

## Review-fix closure

This rerun includes all Task 1 review fixes: isolated clean-room lifecycle and descendant cleanup; the complete Node 20 descriptor, lock, signal, timeout, GPG-link, and unsafe-state matrix; portable INT cleanup; and enforced, measured Darwin isolation observations for the real OpenCode process. Neither this evidence nor the partial musl/OpenCode evidence records a real-home path, credentials, headers, tokens, or configuration bytes.

## Targeted Task 1 verification

| Check | Result | Measured duration |
| --- | --- | --- |
| Release contracts/spikes/registry Bun tests | `26 pass`, `0 fail`, `108` expectations | `0.313s` |
| Clean-room plus Node-wrapper Bats | `30 pass`, `0 skip` | `54.922s` |
| Fresh exact Node wrapper command | Node `v20.0.0` | `0.646s` |
| Fresh exact Node-loaded npm CLI command | npm `9.6.4` | `0.589s` |
| Fresh exact Node wrapper reuse assertion | passed | `0.356s` |
| ShellCheck baseline command | passed | serial baseline |

The three Node commands were separate fresh wrapper processes after the Bats suite. They reused the validated exact descriptor/root, exported absolute Node/npm paths to each child, and were later closed with `tests/release/with-node20.sh --cleanup`.

## Immutable musl inputs and results

| Target | Builder | Runtime manifest | Result | Duration |
| --- | --- | --- | --- | --- |
| `aarch64-unknown-linux-musl` / `linux/arm64` | `rust:1.96.1-alpine@sha256:ccba3c5d98fc76a5ac6eade9bcbbb946635657457c3d269982396633a66da08d` | `docker.io/library/debian@sha256:9b67294679b30e5d6ab257b40594feeb4a4b81f7fcf4131f4decf0d6a212a9b0` | static ELF, matching execution, tmux: PASS | `74.391s` |
| `x86_64-unknown-linux-musl` / `linux/amd64` | `rust:1.96.1-alpine@sha256:f5c84c3751de59f0f318acfbed8b2d04693a12d9171f15835d9c11c9ddcf52db` | `docker.io/library/debian@sha256:63a496b5d3b99214b39f5ed70eb71a61e590a77979c79cbee4faf991f8c0783e` | static ELF, matching execution, tmux: PASS | `113.980s` |

```text
aarch64-unknown-linux-musl elf-static=PASS execution=PASS tmux=PASS
runtime-image-id=sha256:dd619d485f2dbf1d339d8311aae79b426b622f8e5e0da04c2cf0f51125a220dd
runtime-uname=aarch64; tmux=3.6

x86_64-unknown-linux-musl elf-static=PASS execution=PASS tmux=PASS
runtime-image-id=sha256:1970a31143081f71bf062c0ed3fe5fa2f46faa6520e53065138aa3fac2cb7ca9
runtime-uname=x86_64; tmux=3.6
```

The runner verified the matching image architecture, `file` architecture, no `PT_INTERP`, no `NEEDED`, static/non-dynamic `ldd`, clean-room execution, and isolated tmux smoke for each target.

Bootstrap networking is permitted only to obtain already-pinned builder/runtime inputs, the checksum-verified tmux source, and locked Cargo dependencies via `cargo fetch --locked` on bridge networking. The matching-architecture offline compile, ELF inspection, executable smoke, and tmux smoke each use `--network=none`. This is distinct from the OpenCode application run, which is confined to its loopback registry.

## Exact sandboxed OpenCode 1.17.20 result

The clean-room command used `TMUX_BIN=/opt/homebrew/bin/tmux`, normalized the pre-provisioned binary to `1.17.20`, and completed in `4.058s` (`2026-07-24T15:28:46Z`–`2026-07-24T15:28:50Z`).

```text
name=@xiopt/pane-dash-opencode rawSpec=0.1.0 status=PASS cleanup=PASS public-network-requests=0 real-home-writes=0 default-tmux-uses=0
```

Measured isolation observations (asserted by the runner, not inferred from configuration):

```text
platform=darwin
policy-sha256=478bc2db87db01a281c38278eb0ce608656236b15c14df5fde3fee1bad041a11
non-loopback-connect=denied-by-policy
loopback-registry-connect=succeeded
synthetic-root-write=succeeded
forbidden-sibling-write=denied-by-policy
public-network-requests=0
real-home-writes=0
default-tmux-uses=0
```

The resolver request inventory was exactly these four routes, once each:

```text
/@opencode-ai%2fplugin
/%40opencode-ai%2Fplugin/-/plugin-1.17.20.tgz
/@xiopt%2fpane-dash-opencode
/%40xiopt%2Fpane-dash-opencode/-/pane-dash-opencode-0.1.0.tgz
```

The scoped parser produced `@xiopt/pane-dash-opencode@0.1.0`. Before releasing synchronous cleanup, the plugin published nonempty status and a fresh ten-digit heartbeat. Cleanup issued exactly one direct `set-option -pu` for each owned pane option, then the isolated pane, session, and server were observed absent.

## Full baseline, diagnostics, and cleanup

All 18 existing baseline commands ran independently and serially, including `cargo test -- --ignored --nocapture --test-threads=1`; every command exited zero. The baseline covered build, formatting, Clippy, ShellCheck, all Bats, PTY, real shell/tmux integrations, OpenCode-plugin Bun tests, source packaging, focused config startup, active Rust tests, and serial ignored Rust tests.

- Current measured inventory: `466` active Rust tests, `14` ignored Rust tests, `110` full-suite Bats tests, and `26` targeted Task 1 Bun tests.
- `aft_inspect` reported no errors or warnings for the Task 1 scope; `git diff --check` passed before the evidence edit.
- Gate cleanup took `1.252s`: Node descriptor/state directory absent; Task 1 Docker containers `0`, volumes `0`, and temporary runtime images `0`. Pre-existing unrelated Docker resources were left untouched.
