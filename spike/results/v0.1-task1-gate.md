# Task 1 empirical stop-gate — PASS

- Gate source commit: `cfb0447a07875abc70953e47d58419bb713f6460` (including `bb3702e` Node flock/environment recovery).
- Gate source tree: `e9403d0cd7b78cce1b3a08c93900b41ea4a8cc2d` (`git rev-parse <source>^{tree}`).
- Gate run (UTC): `2026-07-24T21:08:49Z`; macOS `15.7.7` / `arm64`; Bun `1.3.14`; Rust/Cargo `1.96.1`; Docker `29.4.0`; Bats `1.13.0`; system Bash `3.2.57`; ShellCheck `0.11.0`; tmux `3.7b`.
- The source tree was clean before the gate and was clean after it. This evidence is the only tracked post-gate change. No raw HOME paths, credentials, headers, tokens, or configuration bytes are recorded.

## Focused release, shell, and Node gates

| Independent check | Result | Duration |
| --- | --- | --- |
| clean-room `bun test scripts/release/tests` | `42 pass`, `0 fail`, `200` expectations | `2.60s` |
| clean-room `bun test --cwd opencode-plugin` | `41 pass`, `0 fail`, `64` expectations | `0.077s` |
| complete Bats (`tests/*.bats`, `tests/release/*.bats`, `scripts/release/tests/*.bats`) | `150 pass`, `0 skip` | passed |
| system Bash 3.2 syntax and ShellCheck, including release wrappers | passed | passed |
| exact Node first provision, two fresh-process reuses, and cleanup | Node `v20.0.0`; npm `9.6.4`; descriptor, root, and result handshake absent after `--cleanup` | passed |

The exact Node gate acquired Node only through isolated mise state, then reused the validated physical descriptor from two separate wrapper invocations. The wrapper's auto-released inherited flock and per-call mode-0600 result handshake were exercised; no state or handshake remained after cleanup.

## Both actual static-musl gates

The arm64 target completed and cleaned before amd64 started. Each target used matching pinned Rust and Debian platform manifests. Bootstrap-only network access fetched locked Cargo inputs and the tmux `3.6` source; its resolved URL and SHA-256 `136db80cfbfba617a103401f52874e7c64927986b65b1b700350b6058ad69607` were verified. Rust compilation, tmux compilation, final assembly, and every runtime check used `--network=none`.

| Target / platform | Result | Duration | Provenance |
| --- | --- | --- | --- |
| `aarch64-unknown-linux-musl` / `linux/arm64` | `elf-static=PASS execution=PASS tmux=PASS` | `93.60s` | Rust `sha256:ccba3c5d98fc76a5ac6eade9bcbbb946635657457c3d269982396633a66da08d`; Debian `sha256:9b67294679b30e5d6ab257b40594feeb4a4b81f7fcf4131f4decf0d6a212a9b0`; `uname=aarch64`; tmux `3.6` |
| `x86_64-unknown-linux-musl` / `linux/amd64` | `elf-static=PASS execution=PASS tmux=PASS` | `166.87s` | Rust `sha256:f5c84c3751de59f0f318acfbed8b2d04693a12d9171f15835d9c11c9ddcf52db`; Debian `sha256:63a496b5d3b99214b39f5ed70eb71a61e590a77979c79cbee4faf991f8c0783e`; `uname=x86_64`; tmux `3.6` |

For both targets, `file` reported the expected architecture, `readelf` found neither `PT_INTERP` nor `NEEDED`, `ldd` reported a static/non-dynamic executable, the current binary completed its three-identity smoke against an isolated tmux socket, and the tmux runtime smoke passed. Cleanup verified no owned containers remained before removing owned volumes, then removed runtime and bootstrap images. No owned containers, volumes, or temporary runtime/bootstrap images remained.

## Sandboxed exact OpenCode 1.17.20

The Darwin Seatbelt clean-room gate passed in `4.19s` using exact OpenCode `1.17.20` with SHA-256 `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`.

```text
name=@xiopt/pane-dash-opencode rawSpec=0.1.0 status=PASS cleanup=PASS public-network-requests=0 real-home-writes=0 default-tmux-uses=0
platform=darwin
policy-sha256=e2a72f00ffbac5b38b4bdcb2b6559d59de403998c9b99c81e5bc67c9e7197506
non-loopback-connect=denied-by-policy
loopback-registry-connect=succeeded
synthetic-root-write=succeeded
forbidden-sibling-write=denied-by-policy
```

The plugin loaded from a temporary packed fixture through the loopback-only registry, published nonempty status and a fresh ten-digit heartbeat before the causal cleanup marker, and made exactly one direct `set-option -pu` cleanup for every owned pane option. All options were absent while the pane remained, then the isolated pane, session, and tmux server were absent. The exact registry inventory was four one-time GETs:

```text
/@opencode-ai%2fplugin
/%40opencode-ai%2Fplugin/-/plugin-1.17.20.tgz
/@xiopt%2fpane-dash-opencode
/%40xiopt%2Fpane-dash-opencode/-/pane-dash-opencode-0.1.0.tgz
```

## Full baseline, counts, and diagnostics

All 18 baseline commands completed independently with status zero. The serial ignored command used `--test-threads=1`.

| # | Check | Duration |
| --- | --- | --- |
| 1 | offline `make build` | `0.993s` |
| 2 | active Rust all-targets | `8.333s` |
| 3 | serial ignored Rust | `24.494s` |
| 4 | Clippy (`-D warnings`) | `0.426s` |
| 5 | rustfmt check | `0.227s` |
| 6 | ShellCheck | `2.752s` |
| 7 | project Bats | `36.025s` |
| 8 | PTY helper | `0.440s` |
| 9 | integration | `1.617s` |
| 10 | pane-dash integration | `13.311s` |
| 11 | Rust engine integration | `120.485s` |
| 12 | Rust engine quoting integration | `2.732s` |
| 13 | Rust live integration | `73.562s` |
| 14 | OpenCode Bun tests | `0.190s` |
| 15 | source-package verification | `20.830s` |
| 16 | focused config startup | `2.703s` |
| 17 | README Bats | `4.206s` |
| 18 | `git diff --check` | `0.024s` |

Active execution passed `452` Rust tests; serial ignored execution passed `14`. Named libtest-list counting produced `466` discovered and `14` ignored, therefore `452` active + `14` ignored = `466` discovered. The count filters `--list` output to named `: test` records, excluding Cargo's non-test status lines and Criterion benchmark entries.

`aft_inspect` reported zero diagnostics. `git diff --check` passed before this evidence edit. Final cleanup ran `make clean` and exact-Node cleanup; Docker cleanup confirmed no owned containers, volumes, or temporary runtime/bootstrap images. No Task 1 temporary roots, Node descriptor/handshake, or owned gate processes remained.
