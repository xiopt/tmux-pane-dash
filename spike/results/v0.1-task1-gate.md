# Task 1 empirical stop-gate — PASS

- Gate UTC: `2026-07-24T16:40:22Z`–`2026-07-24T16:54:17Z`.
- Source commit: `b8ca787e27ed3eed1ab27f055268b1de7f935c78`.
- Source tree: `665abc1a58bcb05a7dc73c8d4ec32f4291b70ee1` (`git rev-parse b8ca787^{tree}`).
- The harness was clean at that source commit before the gate. The post-gate diff contains this evidence file only; the following commit changes evidence only.
- Host/tooling: macOS `15.7.7` / `arm64`; Bun `1.3.14`; Rust/Cargo `1.96.1`; Docker `29.4.0`; Bats `1.13.0`; ShellCheck `0.11.0`.
- Host tmux: `tmux 3.7b`, SHA-256 `17ed04318c6fd2b12e04bce0130425cbe484cbef9cffd2febfba90425e5ad7d0`.
- Exact OpenCode: `1.17.20`, SHA-256 `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`.

## Task 1 unit, lifecycle, and Node 20 gates

| Independent check | Result | Measured duration |
| --- | --- | --- |
| `scripts/release/clean-room.sh -- bun test scripts/release/tests` | `37 pass`, `0 fail`, `154` expectations | `2.288s` |
| `bats scripts/release/tests/clean-room.bats tests/release/with-node20.bats tests/*.bats` | `144 pass`, `0 skip`; includes `34` release clean-room/Node lifecycle tests | `133.788s` |
| ShellCheck plus `bash -n` for baseline and release shell files | passed | `3.126s` |
| `tests/release/with-node20.sh -- node --version` | Node `v20.0.0` provisioned | `4.843s` |
| `tests/release/with-node20.sh -- sh -c 'exec "$NODE_20_BIN" "$NPM_20_CLI" --version'` | npm `9.6.4` in a fresh wrapper process | `0.648s` |
| `tests/release/with-node20.sh -- sh -c 'test "$("$NODE_20_BIN" --version)" = "v20.0.0"'` | validated descriptor/root reuse in a fresh process | `0.396s` |
| `tests/release/with-node20.sh --cleanup` | validated descriptor/root closure | `0.675s` |

The lifecycle suite independently proved synthetic ambient-state isolation, short isolated tmux/TMP roots, HUP/INT/TERM and descendant reaping, a TERM-ignoring descendant, portable Bash 3.2 behavior, stale/live lock handling, descriptor ownership/mode/schema/path validation, physical temporary-root containment, fresh-process reuse, provision timeout cleanup, child signal forwarding, and idempotent cleanup. No actual HOME path, cache path, credential, header, token, or configuration bytes are recorded here.

## Both actual static-musl targets

Each command used the pinned matching-architecture builder and runtime manifests; compilation and execution smoke were run with `--network=none` after permitted pinned bootstrap acquisition.

| Command target | Result | Duration |
| --- | --- | --- |
| `aarch64-unknown-linux-musl` / `linux/arm64` | static ELF, matching execution, isolated tmux: PASS | `65.209s` |
| `x86_64-unknown-linux-musl` / `linux/amd64` | static ELF, matching execution, isolated tmux: PASS | `102.586s` |

```text
aarch64-unknown-linux-musl elf-static=PASS execution=PASS tmux=PASS
runtime-image-id=sha256:367fcbc5525977513497a1c95fa8707f54ba51d11e1462c69239d2f3bb9dd5c7
runtime-manifest=docker.io/library/debian@sha256:9b67294679b30e5d6ab257b40594feeb4a4b81f7fcf4131f4decf0d6a212a9b0
runtime-uname=aarch64; tmux=3.6

x86_64-unknown-linux-musl elf-static=PASS execution=PASS tmux=PASS
runtime-image-id=sha256:fe7e55f9f2c8a9f1b10f247c42b6ab17a92a98095aa46dd08922fafb5de378fd
runtime-manifest=docker.io/library/debian@sha256:63a496b5d3b99214b39f5ed70eb71a61e590a77979c79cbee4faf991f8c0783e
runtime-uname=x86_64; tmux=3.6
```

The runner verified matching image architecture, `file` architecture, no `PT_INTERP`, no `NEEDED`, static/non-dynamic `ldd`, clean-room execution, and isolated tmux smoke for both targets. It removed only its named containers, volumes, and temporary runtime images.

## Exact sandboxed OpenCode 1.17.20

The following clean-room command used the exact checked binary, an absolute tmux executable, a temporary packed plugin, a loopback-only registry, a unique non-default tmux socket, and an asserted Darwin Seatbelt policy. It completed in `3.961s` (`2026-07-24T16:46:01Z`–`2026-07-24T16:46:05Z`).

```text
name=@xiopt/pane-dash-opencode rawSpec=0.1.0 status=PASS cleanup=PASS public-network-requests=0 real-home-writes=0 default-tmux-uses=0
```

Measured isolation observations:

```text
platform=darwin
policy-sha256=1a63038f0cd934e17c6e0fc04249b0243ef6c59fb8095b8a257b7e244712de38
non-loopback-connect=denied-by-policy
loopback-registry-connect=succeeded
synthetic-root-write=succeeded
forbidden-sibling-write=denied-by-policy
public-network-requests=0
real-home-writes=0
default-tmux-uses=0
```

The loopback resolver inventory was exactly these four routes, once each:

```text
/@opencode-ai%2fplugin
/%40opencode-ai%2Fplugin/-/plugin-1.17.20.tgz
/@xiopt%2fpane-dash-opencode
/%40xiopt%2Fpane-dash-opencode/-/pane-dash-opencode-0.1.0.tgz
```

Before synchronous cleanup was released, the plugin published nonempty status and a fresh ten-digit heartbeat. Cleanup issued exactly one direct `set-option -pu` for each owned pane option, then the isolated pane, session, and server were observed absent.

## Full baseline, authoritative counts, diagnostics, and cleanup

All 18 existing baseline commands were run independently. This included clean/build, readme and all-project Bats, ShellCheck, the PTY helper, rustfmt, Clippy, active Rust tests, five real shell/tmux integrations, OpenCode-plugin Bun tests, source packaging, focused config startup, and the serial ignored-Rust command:

```bash
CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml -- --ignored --nocapture --test-threads=1
```

Every baseline command exited zero. The active all-target test execution passed `452` tests; the separate serialized ignored execution passed `14` tests.

The authoritative discovery count intentionally distinguishes all discovered tests from the active run. These were the exact counting commands:

```bash
CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml --all-targets -- --list \
  | python3 -c 'import re,sys; print(sum(bool(re.search(r": test$", line)) for line in sys.stdin.read().splitlines()))'
CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml --all-targets -- --ignored --list \
  | python3 -c 'import re,sys; print(sum(bool(re.search(r": test$", line)) for line in sys.stdin.read().splitlines()))'
```

They produced `466` total discovered and `14` ignored; therefore all-targets has `452` active passed + `14` ignored = `466` total discovered. `libtest --list` does not annotate ignored tests, so the ignored-only listing is required for this calculation.

`aft_inspect` reported no diagnostics for the Task 1 scope before the gate. `git diff --check` passed on the clean source tree before the evidence edit. Task 1 cleanup found `0` named Docker containers, `0` named volumes, `0` temporary runtime images, no Node descriptor/state directory, and `0` Task 1 temporary roots; unrelated Docker resources were not touched.
