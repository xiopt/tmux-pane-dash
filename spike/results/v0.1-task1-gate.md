# Task 1 empirical stop-gate — PASS

- Gate source commit: `5376b690bb1d249f15b37a9ea7d6694200e82e1a` (including Node recovery `61954cd`).
- Gate source tree: `0c1496bf494bced09c3ad2847b6f5db3586288bf` (`git rev-parse <source>^{tree}`).
- The tree was clean before the gate and was clean again after generated-output, Node-state, and owned-resource cleanup. The only post-gate tracked change is this evidence file.
- Host/tooling: macOS `15.7.7` / `arm64`; Bun `1.3.14`; Rust/Cargo `1.96.1`; Docker `29.4.0`; Bats `1.13.0`; ShellCheck `0.11.0`; tmux `3.7b`.

## Release, lifecycle, and exact Node 20 gates

| Independent check | Result | Measured duration |
| --- | --- | --- |
| `clean-room -- bun test scripts/release/tests` | `40 pass`, `0 fail`, `176` expectations | `2.670s` |
| `bats scripts/release/tests/*.bats tests/release/*.bats tests/*.bats` | `147 pass`, `0 skip` | `223.022s` |
| Bash 3.2 syntax plus ShellCheck for baseline and release shell files | passed | `3.893s` |
| exact Node provision, fresh-process npm reuse, fresh-process descriptor reuse, and cleanup | Node `v20.0.0`; npm `9.6.4`; descriptor/root removed | passed |

The complete lifecycle suite includes iteration-3 stale-lock recovery, concurrent recovery serialization, safe owned-root disposal and refusal of unsafe descriptor ownership, successful mise process-group draining (including TERM-ignoring descendants), signal/timeout reaping, and idempotent cleanup. The actual exact-Node command provisioned once, reused the validated descriptor in two new processes, then removed its descriptor and physical temporary root with `--cleanup`.

## Both actual static-musl targets

The gates ran sequentially: arm64 completed and cleaned its owned containers before amd64 began. Each compile and runtime execution used `--network=none` after the pinned bootstrap acquisition; each runner verified matching runtime architecture, static ELF (`file`, no `PT_INTERP`, no `NEEDED`, static `ldd`), executable smoke, and tmux `>=3.6`.

| Target / platform | Result | Duration | Runtime metadata |
| --- | --- | --- | --- |
| `aarch64-unknown-linux-musl` / `linux/arm64` | `elf-static=PASS execution=PASS tmux=PASS` | `71.569s` | builder `sha256:ccba3c5d98fc76a5ac6eade9bcbbb946635657457c3d269982396633a66da08d`; Debian `sha256:9b67294679b30e5d6ab257b40594feeb4a4b81f7fcf4131f4decf0d6a212a9b0`; `uname=aarch64`; tmux `3.6` |
| `x86_64-unknown-linux-musl` / `linux/amd64` | `elf-static=PASS execution=PASS tmux=PASS` | `194.526s` | builder `sha256:f5c84c3751de59f0f318acfbed8b2d04693a12d9171f15835d9c11c9ddcf52db`; Debian `sha256:63a496b5d3b99214b39f5ed70eb71a61e590a77979c79cbee4faf991f8c0783e`; `uname=x86_64`; tmux `3.6` |

Cleanup was dependency ordered for each target: force-remove all named containers, verify their absence, remove both owned volumes, then remove the temporary runtime image. Final owned Docker inventory was `0` containers, `0` volumes, and `0` temporary runtime images; unrelated Docker resources were not touched.

## Exact sandboxed OpenCode 1.17.20

The actual clean-room/Seatbelt gate completed in `4.019s` with the exact OpenCode `1.17.20` binary SHA-256 `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`.

```text
name=@xiopt/pane-dash-opencode rawSpec=0.1.0 status=PASS cleanup=PASS public-network-requests=0 real-home-writes=0 default-tmux-uses=0
platform=darwin
policy-sha256=662f420c77db90966caf66c6bcd1c2dac2708b90c49242e4dcc85a98eba494a0
non-loopback-connect=denied-by-policy
loopback-registry-connect=succeeded
synthetic-root-write=succeeded
forbidden-sibling-write=denied-by-policy
```

The gate required a nonempty status and fresh ten-digit heartbeat before opening the causal cleanup marker. It then observed exactly one direct `set-option -pu` cleanup for each owned pane option (`@pane_dash_status`, `@pane_dash_status_since`, `@pane_dash_heartbeat`, `@pane_dash_title`, and `@pane_dash_model`), verified all options absent while the pane remained present, then observed the isolated pane, session, and tmux server absent.

The exact local registry inventory was four GETs, once each:

```text
/@opencode-ai%2fplugin
/%40opencode-ai%2Fplugin/-/plugin-1.17.20.tgz
/@xiopt%2fpane-dash-opencode
/%40xiopt%2Fpane-dash-opencode/-/pane-dash-opencode-0.1.0.tgz
```

Exact packed fixtures were asserted before loading:

```text
@xiopt/pane-dash-opencode@0.1.0: type=module main=./dist/index.js engines.opencode=>=1.17.20
exports={".":"./dist/index.js","./server":"./dist/index.js"}
files=["dist/index.js","README.md","LICENSE"]
inventory=["package/package.json","package/README.md","package/LICENSE","package/dist/index.js"]

@opencode-ai/plugin@1.17.20: type=module exports={".":"./index.js"} files=["index.js"]
inventory=["package/package.json","package/index.js"]
```

## Full baseline, counts, diagnostics, and final cleanup

All 18 baseline commands completed independently with zero exit status, including clean/build, README and project Bats, ShellCheck, PTY helper, rustfmt, Clippy, active Rust tests, all five real shell/tmux integrations, plugin Bun tests, source-package verification, focused config startup, and the serial ignored command:

```bash
CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml -- --ignored --nocapture --test-threads=1
```

The active all-target execution passed `452` tests; the serial ignored execution passed `14`. Authoritative `--list` counting produced `466` discovered and `14` ignored, therefore `452` active + `14` ignored = `466` discovered. `libtest --list` does not annotate ignored tests, so the ignored-only listing is required for that calculation.

`aft_inspect` reported no source diagnostics before the gate. `git diff --check` and `git diff --exit-code` passed on the clean source tree before this evidence edit. Final cleanup ran `make clean` and exact-Node cleanup; no Node descriptor/state directory, Task 1 temporary roots, or owned Docker resources remained.
