# tmux-pane-dash v0.1 local Stage A closure evidence

- Evidence recorded: `2026-07-28T13:32:32Z` (UTC).
- Overall implementation base SHA: `7bc976a3b71df8ee65de2ef254adba5d51ee3a12`.
- Task 15 base SHA: `e8669cf5b38cd4ce849e97abb9a90a60da9832ed`.
- Final-preclosure SHA: `e8669cf5b38cd4ce849e97abb9a90a60da9832ed` (the tracked
  tree was unchanged before the Task 15 closure files were added).
- Repository: local checkout only; `git remote` is empty and no tag, release,
  package, repository, protection rule, environment, or remote was created or
  changed.
- Local Stage A result: every executed mandatory closure command passed after
  the documented wrapper/cleanup corrections below. The unavailable runner and
  Docker rows are explicit user waivers, not skips represented as passes.
- `Stage B: PENDING EXPLICIT APPROVALS`.

## Host and tool identities

| Tool / identity | Observed value |
| --- | --- |
| Host | macOS 15.7.7, `arm64` |
| Git | 2.54.0 |
| Bun | 1.3.14 |
| Rust | `rustc 1.96.1 (31fca3adb 2026-06-26)`, Cargo 1.96.1, rustup 1.29.0 |
| Node floor | `v20.0.0` |
| npm paired with Node floor | 9.6.4 |
| Host Node/npm | `v24.12.0` / 11.6.2 |
| tmux | 3.7b |
| Bats | 1.13.0 |
| ShellCheck | 0.11.0 |
| OpenCode pinned/latest local executable | 1.17.20 / 1.17.20 |
| Docker | 29.4.0 (not used for the waived Task 1 image reruns) |

Rust commands used `with-rust.sh -> clean-room.sh`, Bun commands used the
validated absolute Bun 1.3.14 executable, parser-dependent commands used
`with-npa.sh`, and Node-dependent commands used `with-node20.sh`. The packed
CLI fixture additionally required isolated `CARGO`/`RUSTC`; those validated
Rust paths were handed to the clean room without weakening its environment
filter. No credential or user-home path is included in this evidence.

## Task 1 — empirical musl and OpenCode stop gates

- Prior review decision inherited at this base: **APPROVED**, represented by
  the checked-in Task 1 evidence lineage (`348e558`, `1e00082`, and the
  subsequent lifecycle/strict-environment fixes). Task 15 did not re-review
  the prior task.
- Available local/no-network checks:
  - Focused contracts/spikes/local-registry Bun command: **43 pass, 0 fail,
    199 expectations**.
  - Current full Bats command: **125 pass, 0 fail, 0 skip**.
  - Pinned OpenCode executable validation: **PASS**; version probe and
    normalization both returned exactly `1.17.20`.
  - Available OpenCode spike: **PASS** —
    `name=@xiopt/pane-dash-opencode rawSpec=0.1.0 status=PASS cleanup=PASS
    non-loopback-connect-errno=EPERM public-network-requests=0
    real-home-writes=0 default-tmux-uses=0`; executable SHA-256
    `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`;
    loopback request count 4, with only the two exact package metadata/tarball
    pairs.
- **WAIVED BY USER / unavailable — not executed and not claimed PASS:** the
  exact Task 1 Docker image inspections and musl reruns:
  - `docker image inspect rust:1.96.1-alpine@sha256:f5c84c3751de59f0f318acfbed8b2d04693a12d9171f15835d9c11c9ddcf52db`
  - `docker image inspect rust:1.96.1-alpine@sha256:ccba3c5d98fc76a5ac6eade9bcbbb946635657457c3d269982396633a66da08d`
  - `docker image inspect debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818`
  - `scripts/release/spikes.ts --musl aarch64-unknown-linux-musl --network=none`
  - `scripts/release/spikes.ts --musl x86_64-unknown-linux-musl --network=none`
  The preprovisioned Task 1 Docker images/rerun resources were unavailable;
  the user expressly waived these rows. Historical Task 1 evidence is not
  reclassified as a current execution.

## Task 2 — identity, legal, workspace, and version synchronization

- Prior review decision inherited at this base: **APPROVED** (`3054dabb`, with
  the mandatory-identity correction `3d23397`).
- `VERSION`, root workspace, Cargo, package metadata, and release identity all
  resolve to `0.1.0`; `version-sync: 0.1.0 (v0.1.0) PASS`.
- MIT identity is `Copyright (c) 2026 xiopt`; no publication occurred.

## Task 3 — Rust version ordering and Rust-only tmux runtime

- Prior review decision inherited at this base: **APPROVED** (`3c82707`).
- Active Rust inventory: **455 passed, 0 failed, 14 ignored, 469 discovered**;
  the serial ignored command used `--test-threads=1` and passed all 14.
- Standalone `version` process test: **1 pass, 0 fail**; exact `pane-dash
  0.1.0` ordering/no-tmux behavior passed.
- The Task 1 record was 453 active / 14 ignored. The named post-Task-1 active
  additions are `parses_version_before_identity_and_rejects_mixed_arguments`
  and `version_is_recognized_before_bench_identity_and_tmux`; no Rust test was
  removed in this interval.
- Rust fmt and Clippy (`-D warnings`) both passed.

## Task 4 — deterministic release artifact builder and verifier

- Prior review decision inherited at this base: **APPROVED** (`e1881d4`, with
  artifact identity/epoch/schema corrections through `8f769b5`).
- Two independent local-fixture builds using tag commit `7bc976a` were
  byte-identical; both build commands and artifact verification returned
  `archives=4 assets=6 inventories=exact reproducible=PASS`.

## Task 5 — Node 20 CLI shell, parsing, platform, and downgrade policy

- Prior review decision inherited at this base: **APPROVED** (`ec91081`, with
  immutable manifest and packed-artifact fixes).
- Exact Node floor assertion passed with `process.version === "v20.0.0"`.
- CLI package dry-run inventory and integrity are recorded below; no lifecycle
  or public endpoint was invoked.

## Task 6 — secure acquisition, tar parsing, and staged verification

- Prior review decision inherited at this base: **APPROVED** (`b61ef2e`, with
  acquisition security, archive bound, fault-boundary, and stream corrections
  `d642bf6`, `75b1823`, `dd4edd4`, `5f333db`, and `46b091d`).
- The release Bun suite passed all acquisition corruption, redirect, size,
  hash, truncation, and cleanup tests; the current explicit acquisition matrix
  contains 11 hostile payload mutations and 4 named operational fault rows,
  all passing.

## Task 7 — managed root, lock, journal, recovery, and transaction cutover

- Prior review decision inherited at this base: **APPROVED** (`23021ea`, with
  durable-recovery correction `6d83c6b`).
- Transaction fault count: 40 injected failure/crash rows (14 phase boundary
  faults, 6 persisted phase crashes, 12 mutation-boundary crashes, 2 recovery
  collision windows, 2 ownership rollback faults, and 4 collision/signal
  rows); all passed in the package suite.

## Task 8 — atomic config files, tmux marker, and lossless OpenCode JSONC

- Prior review decision inherited at this base: **APPROVED** (`e9c6811`,
  `ce79e57`, and `8984faa`).
- Config, symlink, mode, JSONC/comment, marker, concurrent-edit, and
  same-inode tests passed in the release Bun suite; no real HOME or default
  tmux state was touched.

## Task 9 — migration, setup, update, and minimal uninstall

- Prior review decision inherited at this base: **APPROVED** (`44687f9`, with
  ownership-boundary correction `a2ecaba`).
- Packed lifecycle passed `setup -> doctor -> reuse -> update -> old popup ->
  new popup -> doctor -> uninstall`, downgrade refusal/allowance, migration,
  rollback, signal, idempotence, and ownership tests.

## Task 10 — strictly offline read-only doctor

- Prior review decision inherited at this base: **APPROVED** (`1521507`, with
  ownership/binding/socket corrections `7cc5dac`, `a54b834`, `b770ea3`, and
  `15b82d4`).
- Doctor fault inventory contains 12 named error rows plus binding/config
  conflict rows; all passed with zero fetches, locks, or mutations.
- Packed production `doctor --json` ran without an injected endpoint or
  manifest override and returned healthy state.

## Task 11 — bundled publishable OpenCode package and compatibility gates

- Prior review decision inherited at this base: **APPROVED** (`e372ba1`,
  `7799c60`, and `c2f60cb`).
- Full OpenCode Bun suite: **47 pass, 0 fail, 127 expectations**, 7 files.
- Pinned and latest-stable rows both resolved to `1.17.20`, both used the same
  executable SHA-256
  `14a4583c9a3685875f011d6dd4dfbd00498893942be0bb1d2c27e30e70144c89`, and
  both reported `status=PASS cleanup=PASS public-network-requests=0`.

## Task 12 — actual packed CLI isolated end-to-end matrix

- Prior review decision inherited at this base: **APPROVED** (`e533960`).
- Current matching runner: **darwin-arm64 / macOS 15.7.7 arm64**. Exact native
  row passed: **2 pass, 0 fail, 119 expectations** with
  `local-pack-install=PASS registry-requests=0 node=v20.0.0
  runtime-file-url=PASS` and
  `production-bin-no-override=PASS setup doctor reuse update old-popup
  new-popup doctor uninstall: PASS public-network-requests=0
  real-home-writes=0 default-tmux-uses=0`.
- **WAIVED BY USER / unavailable — not executed and not claimed PASS:** these
  exact matching-runner rows had no available runner:
  - `TARGET_KEY=darwin-x64 tests/release/with-node20.sh -- scripts/release/clean-room.sh -- bun test packages/tmux-pane-dash/tests/packed-e2e.test.ts`
  - `TARGET_KEY=linux-arm64 tests/release/with-node20.sh -- scripts/release/clean-room.sh -- bun test packages/tmux-pane-dash/tests/packed-e2e.test.ts`
  - `TARGET_KEY=linux-x64 tests/release/with-node20.sh -- scripts/release/clean-room.sh -- bun test packages/tmux-pane-dash/tests/packed-e2e.test.ts`
  These rows remain **WAIVED BY USER/unavailable**, not skipped-as-pass and
  not satisfied by cross-parser execution.

## Task 13 — public documentation, source manifest, and existing gate rewrite

- Prior review decision inherited at this base: **APPROVED** (`5044fe9`, with
  sensitive-source/document binding correction `e8e5873`).
- Full current Bats and source-package results are recorded below.
- Source package result captured immediately before the closure evidence was
  added: `github=required-after-task14`, `offline=bun-warm-cache-pass`, archive
  bytes `3595264`, archive-run SHA-256
  `22cc7ebf15ca65bae2a127bf4a2eca4763059a2d321e93193a3ffd56c0d8a5de`,
  content-manifest SHA-256
  `fd9fde3e99792bcffed971ffdaeb4f22d32bacb7ded65824c3ef1814c792edcf`,
  mode `0755`; generated `dist`, `target`, `node_modules`, and package
  archives were excluded.

## Task 14 — least-privilege CI, tag workflow, weekly compatibility, and local dry-run

- Prior review decision inherited at this base: **APPROVED** (`fb6f4b1`, with
  trust-boundary and runner corrections `b43eac1`, `80d21f3`, and `e8669cf`).
- Targeted workflow/provenance/dry-run test command: **38 pass, 0 fail, 363
  expectations**. It printed
  `pending-get=PASS user-login=PASS sanitized=PASS request-sha256=PASS
  post-200-deployment-array=PASS historical-endpoint-calls=0` and
  `promotion-permissions=contents:write,actions:read,deployments:read
  npm-permissions=unchanged`.
- Dry-run: `archives=4 assets=6 attestation-subjects=6 npm-inventories=2`,
  `verified-handoff=PASS`, verifier size `1408026`, verifier SHA-256
  `2b490c76552b93c6c97472cb0c17c88a48723934f1f99fb8b1b965720a31bc37`,
  `credentials=absent remote-mutations=0 public-network-requests=0`,
  `release-dry-run: PASS`.
- The exact source-package, version-sync, package-check, public-smoke, and
  full workflow commands were independently rerun; all passed.

## Task 15 — full closure evidence, independent verification, and adversarial review

- RED test was intentionally run before evidence creation:
  `bun test scripts/release/tests/closure.test.ts` failed with the required
  `ENOENT` for `spike/results/v0.1-release-gate.md`.
- This file is the production-pending local evidence required to turn that
  test GREEN. After evidence creation, the closure test passed **1/1 with 21
  expectations** before the Task 15 commit.
- The plan's verifier, adversarial reviewer, final overall review, and Node
  cleanup steps are **not executed** here by explicit user instruction;
  primary will perform them.

## Final Stage A expanded gate

All rows below were run independently. `with-rust`, `with-npa`, and
`with-node20` are shown where the clean-room contract requires them; the
absolute Bun command is shown instead of relying on ambient `bun`.

| # | Command/result/count |
| ---: | --- |
| 1 | `with-rust -> clean-room -- CARGO_NET_OFFLINE=true make build`: PASS |
| 2 | `with-node20 -- node -e 'if (process.version !== "v20.0.0") process.exit(1)'`: PASS |
| 3 | `with-rust -> clean-room -- cargo fmt --manifest-path pane-dash/Cargo.toml -- --check`: PASS |
| 4 | `with-rust -> clean-room -- CARGO_NET_OFFLINE=true cargo clippy --manifest-path pane-dash/Cargo.toml --all-targets -- -D warnings`: PASS |
| 5 | `with-rust -> clean-room -- CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml --all-targets`: PASS; 455 active, 14 ignored, 469 discovered |
| 6 | `with-rust -> clean-room -- CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml --test version -- --nocapture`: PASS; 1/1 |
| 7 | `with-rust -> clean-room -- TMUX=/isolated-tmux CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml -- --ignored --nocapture --test-threads=1`: PASS; 14/14 |
| 8 | `clean-room -- shellcheck pane_dash.tmux scripts/*.sh tests/*.sh spike/*.sh spike/probes/*.sh spike/perf/*.sh`: PASS |
| 9 | `clean-room -- spike/tests/00_pty_helper_test.sh`: PASS |
| 10 | `clean-room -- bats tests/*.bats`: PASS; 46/46 |
| 11 | `with-npa -> with-node20 -> clean-room -- Bun test` with the isolated Rust fixture seam: PASS after evidence creation; 281/281 tests, 0 fail, 1984 expectations |
| 12 | `clean-room -- Bun run build`: PASS; 4 archives, 6 assets, exact/reproducible; `release/dist` removed before the next drift-sensitive row |
| 13 | `clean-room -- Bun run version:check`: PASS; `0.1.0` / `v0.1.0` |
| 14 | `with-node20 -> clean-room -- Bun run pack:check`: PASS; exact package inventory |
| 15 | `with-node20 -> clean-room -- Bun run release:dry-run`: PASS; 4/6/6/2 inventory counts, handoff hash/size, zero credentials/mutations/public requests |
| 16 | `clean-room -- tests/integration.sh`: PASS; all integration checks |
| 17 | `clean-room -- tests/pane_dash_integration.sh`: PASS; 4 hostile-label/identity rows |
| 18 | `with-rust -> clean-room -- tests/rust_engine_integration.sh`: PASS |
| 19 | `with-rust -> clean-room -- tests/rust_engine_quoting_integration.sh`: PASS |
| 20 | `with-rust -> clean-room -- tests/rust_live_integration.sh`: PASS; budgets, Task 9, phase 6, creation and cleanup rows |
| 21 | `with-rust -> clean-room -- tests/source_package.sh`: PASS; source digest recorded above |
| 22 | `with-rust -> clean-room -- CARGO_NET_OFFLINE=true cargo test --manifest-path pane-dash/Cargo.toml --test config_startup -- --nocapture`: PASS; 7/7 |
| 23 | Native available `TARGET_KEY=darwin-arm64` packed E2E with `with-node20`, isolated Rust fixture, and clean room: PASS; 2/2, 119 expectations |
| 24 | `with-npa -> with-node20 -> clean-room -- Bun test opencode-plugin/tests/real-opencode.test.ts`: PASS; 1/1, 51 expectations; pinned/latest rows both PASS |
| 25 | `git diff --check`: PASS |

Additional independently required current gates:

- Full current Bats (`tests/*.bats tests/release/*.bats
  scripts/release/tests/*.bats`): **125 pass, 0 fail, 0 skip**.
- Explicit public smoke Bats: **2 pass, 0 fail**.
- Full packed CLI package suite: **97 pass, 0 fail, 1043 expectations**.
- Full OpenCode plugin suite: **47 pass, 0 fail, 127 expectations**.
- Release tooling plus provenance suite (before adding the closure evidence
  row): **136 pass, 0 fail, 796 expectations**; the closure row is included in
  the final 281-test count after evidence creation.
- Task 14 targeted closure suite: **38 pass, 0 fail, 363 expectations**.

## Release fixtures and package inventories

Two independent fixture output directories were built, verified, compared with
`diff -qr`, and removed. The six assets in either byte-identical directory
were:

| Asset | Size | SHA-256 |
| --- | ---: | --- |
| `tmux-pane-dash-v0.1.0-aarch64-apple-darwin.tar.gz` | 880976 | `f0ac0b048d7826ed94594f309c28c0fece4369c156379dcbd7bce0c35e478d60` |
| `tmux-pane-dash-v0.1.0-x86_64-apple-darwin.tar.gz` | 8741 | `ba2eaa797f55e0a49dbbbe413cecc229c96897f75d6986ae03a9cb0449c4a975` |
| `tmux-pane-dash-v0.1.0-aarch64-unknown-linux-musl.tar.gz` | 8739 | `7223b35b4da8eb399270355263054449cfb25fd007d2afb064bf75cae5af7db7` |
| `tmux-pane-dash-v0.1.0-x86_64-unknown-linux-musl.tar.gz` | 8741 | `5d613d1c78930de497f746c6983a60a0a454ff9897d1d2741fb6bc3b5a555917` |
| `release-manifest.json` | 1406 | `d52301bdfd60fdfc8d84adaa5b9adf44524359cd3e29d376c1b389a1f159bccf` |
| `SHA256SUMS` | 474 | `3755be92ef7aaa80a350769357dfd22975143e1bdfba07e2d43f00bb0314eb2d` |

Each of the four archives contained exactly eight files, with two explicit
0755 directory headers (`bin/`, `scripts/`) and these exact file inventory,
types, and modes: `LICENSE` regular 0644, `README.md` regular 0644, `VERSION`
regular 0644, `bin/pane-dash` regular 0755, `manifest.json` regular 0644,
`pane_dash.tmux` regular 0755, `scripts/open.sh` regular 0755, and
`scripts/tag.sh` regular 0755. Binary sizes were 1798048 (darwin-arm64), 8
(darwin-x64 fixture), 70 (linux-arm64 fixture), and 70 (linux-x64 fixture).
There were no symlink, device, hard-link, or other archive entries.

The release manifest had schema 1, repository `xiopt/tmux-pane-dash`, version
`0.1.0`, tag `v0.1.0`, four exact target records, and only immutable
`https://github.com/xiopt/tmux-pane-dash/releases/download/v0.1.0/...` URLs.
The six release subjects are exactly the four archives above plus
`release-manifest.json` and `SHA256SUMS`.

Node 20/npm dry-run packs were executed independently with exact inventories:

| Package | Tarball | Size | Entry paths | Integrity |
| --- | --- | ---: | --- | --- |
| `@xiopt/tmux-pane-dash@0.1.0` | `xiopt-tmux-pane-dash-0.1.0.tgz` | 46513 | `package/LICENSE`, `package/README.md`, `package/dist/cli.js`, `package/dist/runtime.js`, `package/generated/release-manifest.json`, `package/package.json` (6) | `sha512-EJMfs39hFRVgjCNR03opMGXk4w0sXSxZ5ICjLr9gcNrdngAkKuJsayn9ClqJH6tpEaxwu/1OeGfwC3CJixPc7g==` |
| `@xiopt/pane-dash-opencode@0.1.0` | `xiopt-pane-dash-opencode-0.1.0.tgz` | 3781 | `package/LICENSE`, `package/README.md`, `package/dist/index.js`, `package/package.json` (4) | `sha512-tkDcR0W3RX7Heh/gUoqnBBOC2FOQQQo8/2azkEAivLDwmpH4kVl/B9ZnWE7DhdGryZakciX5yRYbDfhh7cEtoQ==` |

The npm dry-run SHA-1 values were respectively
`11f418da1f0fdc447f4d4fc51f5c1c6615def407` and
`b402bea96f71fe27f348e612a37e46b845469418`; both had `bundled=[]`.

## Scan and boundary results

- Credential scan: no matches; exit 1 means no matching lines.
- Plan placeholder scan: no matches; exit 1 means no matching lines.
- Current endpoint/override scan of packed CLI: no matches; exit 1 means no
  matching lines.
- Generated-directory ignore scan: no matches; exit 1 means no matching lines.
- Branding scan had only legitimate migration/test matches at
  `packages/tmux-pane-dash/src/config-tmux.ts:36-38` and
  `scripts/release/tests/docs.test.ts:18`: legacy ownership migration must
  recognize old `@pane-dash-engine`/tmux markers, while the documentation test
  asserts those names are absent from current public docs. No stale branding is
  emitted by the public package/runtime.
- `.gitignore` diff from the normative plan base contains only the reviewed
  `.cortexkit/` entry in addition to existing build outputs; no tmp/scratch,
  reservation, downloaded-draft, or release-final cleanup-evasion path was
  added.
- `release/dist` was removed and verified absent. The two fixture directories
  were removed. No generated package tarball is tracked.
- A post-evidence source-package validation (before the final spec/plan
  closure append) also passed with archive bytes `3617280`, archive-run
  SHA-256 `2ade5d44a3ab3d170b0102bf7814647a0e926f4ac8a59f709fc68c47ad29d10e`,
  and content-manifest SHA-256
  `1a39e994ff969f3b133644fac907c72ca800188bef4b272ca14fe48feb3bf6f5`.
  The canonical source digest above remains the Task 14 pre-closure digest,
  because this evidence file is itself part of the source manifest; recording
  a digest of a file containing that same digest would be self-referential.
- The final post-spec/plan source-package command also passed with archive
  bytes `3619328`, archive-run SHA-256
  `eaa5f537f885356aca77eae6a2a71bacfc703f9fe25dafb1dd30483cbd2fde9b`, and
  content-manifest SHA-256
  `1aef3a74a6d2eeb614b3f369d572ac5f4285bf2bf4b9a4183cc5df2b8fef380f`.
- Final local boundary: no credentials, zero public application requests,
  zero remote mutations, zero default-tmux uses, zero real-home writes, and
  no remote/tag/release publication. The only OpenCode network activity was
  the loopback fixture registry; its four expected requests are listed in the
  Task 1 result.

## Known transient setup observations

These are recorded to avoid turning an intermediate harness failure into a
false pass claim; none remained in the final gate:

1. The first Rust live integration attempt timed out while opening the
   Task-9 normal popup (`timeout: popup 6 control`); the exact command was
   rerun and passed completely. This was a harness timing flake, not a waived
   target row.
2. The first full Bats invocation preserved optional `BATS_BIN` and
   `SHELLCHECK_BIN` through the outer clean room, which correctly exposed the
   exact PATH assertion in `with-rust.bats`; the command was rerun with the
   required minimal clean-room tool set and all 125 tests passed.
3. The first dry-run after the deliberate build row correctly rejected
   generated `release/dist`; the generated directory was removed before the
   independent dry-run row, which then passed. The final tree has no such
   directory.
4. Bun release tests initially reported the expected closure-test ENOENT and
   missing precondition variables before the exact parser/Node/Rust wrappers
   and offline locked development dependency install were supplied. The
   final wrapped suite passed and the dependency directory is removed before
   commit.

No final verifier, adversarial review, final overall review, or persistent
Node cleanup was performed; primary owns those explicitly deferred actions.
